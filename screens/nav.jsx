import { StyleSheet, Text, View } from 'react-native'

const Nav = () => {
  return (
    <View>
      <Text style={{height: 20}}>nav</Text>
    </View>
  )
}

export default Nav

const styles = StyleSheet.create({})